const Chip = ({ text }) => {
  return (
    <span className='inline-block px-2 py-1 ring-1 ring-gray-400 text-gray-600 rounded-full text-xs font-light'>
      {text}
    </span>
  )
}

export default Chip
